# AI 协作说明

AI 协作者处理本仓库时，先读取根 `AGENTS.md`，再读取当前任务相关的 `docs/architecture`、`docs/migration` 和 `.agents/skills`。

`aoi-admin/` 只作为参考资料。允许审计其代码、文档、迁移、配置、React 前端和历史 AI 文档；禁止把旧规则、旧技能正文、Go runtime 路径或插件系统迁入新项目。

长期事实写入 `docs/`；一次性审计和阶段性迁移记录写入 `docs/migration/` 或任务报告。

